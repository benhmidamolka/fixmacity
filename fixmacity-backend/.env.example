# ── FixMaCity Backend — Environment Variables ──────────────────
# Copy to .env and fill in your values

PORT=5000
NODE_ENV=development

# ── JWT ────────────────────────────────────────────────────────
JWT_SECRET=aB3kR9xP2mQ7nL5vT8wY1cD4jF6molka


# ── Local PostgreSQL (pgAdmin) ─────────────────────────────────
# 1. Open pgAdmin → right-click "Databases" → Create → name it "fixmacity"
# 2. Open Query Tool on that database and run schema.sql
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fixmacity
DB_USER=postgres
DB_PASSWORD=98452169.PApa
DB_POOL_MAX=10


BASE_URL=http://localhost:5000
# ── Cloudinary (photo uploads) ────────────────────────────────
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ── Google Gemini (chatbot AI) ────────────────────────────────
GEMINI_API_KEY=AIzaSyB8C1lPSeMa8voTOkEKagwkiak1wI7ttJw

# ── Frontend origin (CORS + email links) ─────────────────────
FRONTEND_URL=http://localhost:3000

# ── Email / Nodemailer ────────────────────────────────────────
# If unset, emails are silently skipped — safe for local dev.
# For Gmail: Account → Security → 2FA → App Passwords
EMAIL_SERVICE=gmail
EMAIL_USER=
EMAIL_PASS=

SUPABASE_URL=https://wcmytoghuyiscvfnjfzm.supabase.co
SUPABASE_SERVICE_KEY=sb_secret_8SnT-9LH7Mc0m_EdxJq-vg_8iWppfTz
